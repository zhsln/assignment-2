package Persons.interfaces;

public interface Payable {

    /*Interface contains getPaymentAmount().
    Overrided method shows Person's payment amount (salary if Employee, scholarship if Student). */
    double getPaymentAmount();
}
